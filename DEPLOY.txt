Piclet Gold on Locket Dio (Render)

Repo: github.com/buiduchuy2010qn-prog/locket-dio
Service: locket-dio (Static Site)

Build (Render Settings nếu cần set tay):
  Build Command:  npm install && npm run build
  Publish Directory: dist

SPA: rewrite /* → /index.html

Local:
  npm install
  npm run dev

Demo login:
  you@piclet.app / demo123  (Free)
  mina@piclet.app / demo123 (Gold)

Note: App branding = Piclet Gold (original). Hosted on existing locket-dio Render URL.
