Locket Dio — Render Static (Blueprint)

URL: https://locket-dio-ly9t.onrender.com

Build (render.yaml):
  cd apps/web && npm install && npm run build && cd ../.. && node scripts/copy-web-dist.mjs

Publish:
  dist

If Blueprint still fails, open Settings (if editable) or re-sync Blueprint.

Manual Deploy → Deploy latest commit after push.
