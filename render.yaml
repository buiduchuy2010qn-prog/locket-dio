services:
  # IMPORTANT: Web Service (Docker/Node), NOT Static Site
  - type: web
    name: locket-dio
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /dio-api/
